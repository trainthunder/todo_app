<%= form_with(model: task, local: false, id: "new_task", class: "flex gap-2") do |form| %>
  <%= form.text_field :title, placeholder: "New task...", class: "flex-1 border rounded px-3 py-2" %>
  <%= form.submit "Add", class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
<% end %>
